import 'package:flutter/material.dart';

const kprimaryColor = Color.fromRGBO(6, 132, 0, 1);

class AppColors {
  
  // Light Mode Colors
  static const Color lightScaffoldColor = Colors.white;
  static const Color lightPrimary = Color.fromRGBO(6, 132, 0, 1);
  static const Color lightCardColor = Color.fromARGB(106, 250, 250, 250);

  // Dark Mode Colors
  static const Color darkScaffoldColor = Color.fromARGB(255, 18, 18, 18);
  static const Color darkPrimary = Color.fromRGBO(3, 79, 0, 1); // Darker shade of kprimaryColor
  static const Color darkCardColor = Color.fromARGB(255, 40, 40, 40);
  static const Color darkTextColor = Colors.white;
}

